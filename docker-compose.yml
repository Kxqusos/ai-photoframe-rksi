services:
  postgres:
    extends:
      file: deploy/compose/docker-compose.dev.yml
      service: postgres

  backend:
    extends:
      file: deploy/compose/docker-compose.dev.yml
      service: backend

  frontend:
    extends:
      file: deploy/compose/docker-compose.dev.yml
      service: frontend

  nginx:
    extends:
      file: deploy/compose/docker-compose.dev.yml
      service: nginx

volumes:
  postgres_data:
  backend_storage:
  backend_logs:
  backend_venv:
  frontend_node_modules:
